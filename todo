- check if watchdog is available on remote machine
- try use common Observer api
- make_observer can be work with both local and remote
- add clasifiers to setup.py
- OSError: inotify watch limit reached
- test for ignored partial paths (lteDo/...)
- ignore glob/regex
- initial ignore
